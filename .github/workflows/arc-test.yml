name: ARC Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: arc-actions-runner-controller
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Test ARC runner
        run: echo "ARC runner is working"
