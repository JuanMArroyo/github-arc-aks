name: ARC Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: aks-arc
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Test ARC runner
        run: echo "ARC runner is working"
